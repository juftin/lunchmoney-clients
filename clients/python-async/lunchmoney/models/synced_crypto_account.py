# coding: utf-8

"""
    Lunch Money API - v2

    ### Introduction  Welcome to the Lunch Money v2 API reference. This is the **v2.11.0** spec.  The API is available at `https://api.lunchmoney.dev/v2`. Get your access token from the [Lunch Money developers page](https://my.lunchmoney.app/developers).   **Try it from these docs**  These docs are interactive — use **Test request** on any endpoint to call the API from this page. Choose a LIVE or MOCK service from the Server dropdown. Requests sent to `https://api.lunchmoney.dev/v2` can <span class=\"red-text\"><strong>change or delete</strong></span> your data and are <span class=\"red-text\"><strong>permanent</strong></span>. See the [Getting Started Guide](https://lunchmoney.dev/v2/getting-started) before using the live API.  **Static mock server**  Explore without risk to real data. Select `https://mock.lunchmoney.dev/v2` in the Server dropdown to work with static mock data. POST, PUT, and DELETE requests will return realistic responses, but do not change the mock data.   **Client Libraries & SDKs**  An official TypeScript SDK is available on [NPM](https://www.npmjs.com/package/@lunch-money/v2-api-spec) and [GitHub](https://github.com/lunch-money/lunch-money-js-v2). For Python or other languages, see [lunchmoney-clients](https://github.com/juftin/lunchmoney-clients) or generate a client from [this OpenAPI spec](/v2/openapi).  **Migrating from v1**  The v2 API is not backwards compatible with v1. See the [Migration Guide](https://lunchmoney.dev/v2/migration-guide) for details.  **Useful links** - [Getting Started Guide](https://lunchmoney.dev/v2/getting-started) - [v2 API Overview](https://lunchmoney.dev/v2/overview) - [Version History](https://lunchmoney.dev/v2/version-history) - [Migration Guide](https://lunchmoney.dev/v2/migration-guide) - [Rate Limits](https://lunchmoney.dev/v2/rate-limits)

    The version of the OpenAPI document: 2.11.0
    Contact: devsupport@lunchmoney.app
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from lunchmoney.models.crypto_synced_balance import CryptoSyncedBalance
from typing import Optional, Set
from typing_extensions import Self

class SyncedCryptoAccount(BaseModel):
    """
    SyncedCryptoAccount
    """ # noqa: E501
    id: StrictInt = Field(description="System defined unique ID for the synced crypto connection")
    provider: StrictStr = Field(description="Provider used for the synced crypto connection")
    status: StrictStr = Field(description="Status of the synced crypto account. If not `active`, see the [Knowledge Base](https://support.lunchmoney.app/setup/crypto#why-is-my-synced-crypto-account-showing-not-supported) for details.")
    created_by_name: Optional[StrictStr] = Field(description="Name of the user who created the crypto connection")
    created_at: datetime = Field(description="Date/time the synced crypto connection was created in ISO 8601 extended format")
    updated_at: datetime = Field(description="Date/time the synced crypto connection was last updated in ISO 8601 extended format")
    last_import: Optional[datetime] = Field(default=None, description="System defined timestamp in ISO 8601 extended format of the last successful import.")
    display_name: Optional[StrictStr] = Field(description="Optional display name for the synced crypto connection")
    balances: List[CryptoSyncedBalance] = Field(description="Balances currently held in the synced crypto connection")
    __properties: ClassVar[List[str]] = ["id", "provider", "status", "created_by_name", "created_at", "updated_at", "last_import", "display_name", "balances"]

    @field_validator('provider')
    def provider_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['kraken', 'coinbase', 'ethereum']):
            raise ValueError("must be one of enum values ('kraken', 'coinbase', 'ethereum')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'unsupported', 'relink', 'initializing']):
            raise ValueError("must be one of enum values ('active', 'unsupported', 'relink', 'initializing')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SyncedCryptoAccount from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in balances (list)
        _items = []
        if self.balances:
            for _item_balances in self.balances:
                if _item_balances:
                    _items.append(_item_balances.to_dict())
            _dict['balances'] = _items
        # set to None if created_by_name (nullable) is None
        # and model_fields_set contains the field
        if self.created_by_name is None and "created_by_name" in self.model_fields_set:
            _dict['created_by_name'] = None

        # set to None if last_import (nullable) is None
        # and model_fields_set contains the field
        if self.last_import is None and "last_import" in self.model_fields_set:
            _dict['last_import'] = None

        # set to None if display_name (nullable) is None
        # and model_fields_set contains the field
        if self.display_name is None and "display_name" in self.model_fields_set:
            _dict['display_name'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SyncedCryptoAccount from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "provider": obj.get("provider"),
            "status": obj.get("status"),
            "created_by_name": obj.get("created_by_name"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at"),
            "last_import": obj.get("last_import"),
            "display_name": obj.get("display_name"),
            "balances": [CryptoSyncedBalance.from_dict(_item) for _item in obj["balances"]] if obj.get("balances") is not None else None
        })
        return _obj


